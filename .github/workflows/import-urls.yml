name: Importa ricette da URLs (NEW)

on:
  # Consente il bottone "Run workflow"
  workflow_dispatch: {}
  # Forza UNA run automatica quando questo file cambia (così appare subito in Actions)
  push:
    branches: [ main ]
    paths:
      - ".github/workflows/import-urls.yml"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Workflow registrato correttamente."
