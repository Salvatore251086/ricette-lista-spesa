name: Bootstrap Actions visibility

on:
workflow_dispatch: {}
push:
branches: [ main ]
paths:
- ".github/workflows/z_bootstrap.yml"

jobs:
ping:
runs-on: ubuntu-latest
steps:
- name: Echo
run: echo "Workflow registrato correttamente"
